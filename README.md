# EncryptedStorage
Node,express,mongoDB backend, with python frontend.

# For Execution
CLOUD_NAME,
CLOUD_API_KEY,
CLOUD_API_SECRET,
MONGO_URI,
JWT_secret,
 these must be defined in .env file
